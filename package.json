{
  "name": "notes-site",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "sh scripts/sync-experience.sh",
    "index": "node scripts/gen-index.mjs",
    "prep": "npm run sync && npm run index",
    "dev": "npm run prep && vitepress dev docs",
    "build": "npm run prep && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
